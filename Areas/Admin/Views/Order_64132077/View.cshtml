@model WebNoiThat_64132077.Models.EF.Order
@using PagedList;
@{
    ViewBag.Title = "View";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
@section header{
    <div class="content-header">
        <div class="row mb-3">
            <div class="col-sm-6">
                <h3 class="m-0">Quản lý đơn hàng</h3>
            </div><!-- /.col -->
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a style="text-decoration: none" href="/Admin/Home_64132077">Home</a></li>
                    <li class="breadcrumb-item active">Chi tiết Đơn hàng</li>
                </ol>
            </div><!-- /.col -->
        </div><!-- /.row -->
    </div>
}
<!-- Main content -->
<section class="content">

    <!-- Default box -->
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Chi tiết đơn hàng</h4>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-4">
                    <div class="form-group">
                        <label>Mã đơn hàng</label>
                        <p class="form-control">@Model.Code</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label>Mã khách hàng</label>
                        <p class="form-control">@Model.CustomerID</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label>Họ tên khách hàng</label>
                        <p class="form-control">@Model.CustomerName</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label>Số điện thoại</label>
                        <p class="form-control">@Model.User.Phone</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label>Địa chỉ giao hàng</label>
                        <p class="form-control">@Model.User.Address</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label>Nhân viên lập</label>
                        <p class="form-control">@Model.EmployeeName</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label>Ngày tạo</label>
                        <p class="form-control">@(Model.OrderDate.HasValue ? Model.OrderDate.Value.ToString("dd/MM/yyyy") : "")</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label>Ngày giao hàng</label>
                        <p class="form-control">@(Model.DeliveryDate.HasValue ? Model.DeliveryDate.Value.ToString("dd/MM/yyyy") : "")</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label>Trạng thái</label>
                        <p class="form-control">
                            @if (Model.Status == 1)
                            {
                                <span>Đang xử lý</span>
                            }
                            @if (Model.Status == 2)
                            {
                                <span>Đang giao hàng</span>
                            }
                            @if (Model.Status == 3)
                            {
                                <span>Hoàn thành</span>
                            }
                        </p>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-12">
                    <h4>Danh sách sản phẩm</h4>
                </div>
                <div class="col-12">
                    @Html.Action("Partial_SanPham", "Order_64132077", new { id = Model.ID })
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label>Tổng tiền</label>
                        <p class="form-control">@WebNoiThat_64132077.Common.Common.FormatNumber(Model.TotalAmount, 0) VNĐ</p>
                    </div>
                </div>
                <div class="col-md-12">
                        <a href="@Url.Action("Print", "Order_64132077", new { id = Model.ID })" target="_blank" class="btn btn-primary">
                            <i class="fa fa-print"></i> In hóa đơn
                        </a>
                </div>
            </div>
        </div>


        <!-- /.card-footer-->
    </div>
    <!-- /.card -->

</section>
<!-- /.content -->
